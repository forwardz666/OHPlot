# 2026-08-01 工作日志：功能落后排查与真机重部署

> 用户反馈"回滚失败，功能全面落后于 P0 执行后"。经排查：代码与构建层面回退正确、功能完整，
> 根因是**设备上安装的是旧 HAP**。重新部署最新 HAP 后真机功能完整。

---

## 工作内容

1. **备份核实**：确认 `backup-20260731-160829-airstars-p0/` 存在且完整（7 个文件，均为 P0 执行前
   07-28/07-31 的状态）；另有更早的 `backup-20260727-010510/`。
2. **回退正确性验证**：OHPlotAbility.ets / MyAbilityStage.ets / qohos.cpp / Index.ets 四个核心文件
   与备份 SHA256 **完全一致**；main.cpp 比备份新（P1b 新增 KeyRelease + insert_row/col 修复，功能增加）。
3. **功能完整性抽查**：libentry.so（07-31 23:53 重建）命令符号抽查全在（newProject/importASCII/
   exportASCII/getUiState/plot2D/smooth/fft/getProjectTree/getPreference/setPreference/pasteSelection/key release）；
   07-30/31 新增组件（ColumnSidebar/ContextMenu/BottomToolBar/Theme/MenuBar/ToolBar/MainLayout）全部存在。
4. **根因定位**：用户观察到的"功能落后"是**设备上运行的是旧 HAP**（此前部署的版本），非代码丢失。
5. **重新部署**：`hdc install -r` 强制重装最新 HAP（90MB，07-31 23:55 构建）→ 重启应用 → 验证。

---

## 完成项

- [x] 备份核实（P0 前备份存在且完整）
- [x] 回退正确性验证（4 文件 SHA256 一致）
- [x] 功能完整性抽查（libentry.so 命令符号 + ArkTS 组件）
- [x] 根因定位（设备运行旧 HAP）
- [x] 真机重部署 + 启动验证（startQtApplication true + 6 桥 + handleWindowEvent ok）
- [x] UI 布局验证（8 菜单项 + 28 工具栏按钮 + TableViewWidget 在位）

## 技术决策

- **决策 1**: 功能落后的根因是部署产物陈旧，非回退失败。回退操作本身验证正确（哈希比对 + 时间戳）。
- **决策 2**: 以 `hdc install -r` 强制重装替代依赖 IDE 的增量部署，确保设备始终运行最新 HAP。

## 文件变更

| 文件 | 变更类型 | 说明 |
|------|---------|------|
| 无代码变更 | - | 本次为诊断 + 重部署，未修改源码 |

## 遗留问题

- 若再次遇到"功能少了"，先 `hdc install -r` 重装最新 HAP 再排查代码。
- 剪贴板 Copy→Paste 回环、Shift 组合键完整映射仍需人工真机验证（见 P1 日志）。

## 环境信息

- 项目: scidavis-ohos
- 设备: HUAWEI MatePad 11.5（OpenHarmony-7.0.0.32, API 26, 192.168.0.116:46817）
- 日期: 2026-08-01
- HAP: entry-default-signed.hap 90,378,141 字节（07-31 23:55 构建）
- 依赖变更: 无

## 后续建议

1. 人工真机验证剪贴板回环与 Shift 组合键（需蓝牙键盘）。
2. 继续 P1 下一项（qt.conf / QT_QPA_PLATFORM / RawfileCopier / HDS）。
