# 2026-08-03 工作日志：批次 2 命令补齐与批次 4 UI 对齐落地

> 任务：备份（排除 hilog）→ 补齐未完成工作（批次 2 命令 + 批次 3 对话框）→ UI 补齐（批次 4）→ 真机侧载验证。
> **核心成果**：①Qt main.cpp 注册 10 命令（批次 2）再注册 11 命令（Agent G），Qwt 5 API 兼容修复；②批次 4 菜单树/Theme Token/12 对话框落地并完成接线；③string.json 两次并行编辑损坏均恢复，最终 511 条三语言对齐、getAppString 覆盖 0 缺失；④代码审查 1 CRITICAL + 7 MAJOR 全部修复落地；⑤ninja 编译 + hvigor 打包 + 真机侧载验证 6/6 PASS。

---

## 工作内容

1. **备份**：backup-20260802-1853-ohos-full，robocopy 排除 hilog/build/oh_modules/.hvigor/.cache 等缓存目录，共 3.25GB、9457 文件。
2. **探索结论**：
   - Qt main.cpp（scidavis/scidavis/src/main.cpp）10 个命令全部缺失
   - ArkTS 侧 BottomToolBar TblGroup 4 按钮已就位，但 table_size 弹"未适配"
   - 菜单缺 Scripting/Format/Tools；Plot/Analysis 子菜单被拍平
   - Theme.ets 仅 14 个常量
   - ColumnSidebar 已在 TablePage 挂载（MainLayout 有意不挂，用户要求 Qt 原生面板）
3. **批次 2（Agent A, deep）**：main.cpp 注册 10 命令 normalize/convolve/getMatrixData/addText/drawArrow/drawLine/timeStamp/addImage/newLegend/tableSize（L1918-2147）；getMatrixData + tableSize 加入 queryCommands()（L619）。
4. **批次 4 菜单树（Agent B, visual-engineering）**：rebuildMenus 新增 Scripting/Format/Tools 菜单；恢复 Plot/Analysis 子菜单层级（Special▾/Statistical▾/QuickFit▾/Smooth▾/FFT Filter▾）；File→New 补项；BottomToolBar isTable 动态化；TableSizeDialog 加入 TableOpsDialog；dispatch 接线。
5. **批次 4 Theme Token（Agent C）**：Theme.ets 常量 14→50（对话框表面/强调色/间距/圆角/阴影/字号/按钮/输入框/Tab），迁移 7 个存量对话框。
6. **批次 4 对话框新建（Agent D 批次 1 + Agent E 批次 2/3）**：新建 12 个对话框 PlotWizardDialog/AxesDialog/AssociationsDialog/CurveRangeDialog/TextDialog/LineDialog/ImageDialog/SymbolDialog/LayerDialog/RenameWindowDialog/FindDialog/TableStatisticsDialog。
7. **集成（visual-engineering）**：12 个对话框挂载到 MainLayout（L1567-1578）+ 12 个 @State + dispatch 重接（add_text/draw_line/draw_arrow/add_image 改为打开对话框；format_* 打开 AxesDialog）。
8. **Agent G（deep）**：再注册 11 命令 plotWizard/getAxisConfig/getPlotAssociations/updatePlotAssociation/getCurveInfo/setCurveRange/arrangeLayers/getActiveWindowInfo/renameWindow/getStartPath/find（L2156-2496），5 个查询加入 queryCommands()；修复 Qwt 5 API 兼容（QwtScaleTransformation::Log10 而非 Logarithmic）+ 补 4 个头文件。
9. **string.json 并行编辑损坏修复（两次）**：第一次 3 agent 并行写坏，脚本合并 git HEAD + 提取恢复为 406 条；第二次 M-6 agent 写坏 base（双重逗号 `},,`），提取恢复 492 条 + 同步 en_US/zh_CN + 补 19 个 sidebar/dlg key；最终 511 条三语言对齐、getAppString 覆盖 0 缺失、无乱码。
10. **代码审查（unspecified-high）**：发现 1 CRITICAL（tableSize 只读无 resize）+ 7 MAJOR（setAxisGrid/setAxisFrame 缺失、setAxisScale 忽略 from/to、getAxisConfig 网格映射反转、LineDialog 参数不匹配、8 对话框不可达、93 i18n key 缺失、Text/Image 样式被丢弃）。
11. **修复落地**：
    - Qt 侧：resizeTable/setAxisGrid/setAxisFrame 新命令；setAxisScale 读取 from/to；getAxisConfig 用 QwtPlot::yLeft/yRight；drawLine/drawArrow 读取 xStart/yStart 契约
    - ArkTS 侧：M-5 接线（8 对话框经菜单可达：View→plot_wizard、Graph→associations/curve_range/symbol/arrange_layers、Windows→rename_window/find_window、Table statistics→对话框）；M-6 补齐 93+19 i18n key
12. **构建**：ninja 编译通过（POST_BUILD cp/mkdir 报 FAILED 属预期），libentry.so 104.8MB 复制到 entry/libs/arm64-v8a；hvigor assembleHap BUILD SUCCESSFUL（18.6s）。
13. **真机侧载**：hdc install -r 成功（org.scidavis.ohos）；aa start OHPlotAbility 成功；hilog 显示完整链路（Qt 主窗口创建 → setChromeInsets → getUiState 返回 Table1）；verify_smoke.py 6/6 PASS。

## 完成项

- [x] 备份（backup-20260802-1853-ohos-full，排除 hilog 与缓存，3.25GB/9457 文件）
- [x] 探索定位（main.cpp 10 命令缺失 + ArkTS 接线现状确认）
- [x] 批次 2：main.cpp 注册 10 命令 + queryCommands 补 2 查询（Agent A）
- [x] 批次 4 菜单树：Scripting/Format/Tools + Plot/Analysis 子菜单层级恢复 + BottomToolBar isTable 动态化（Agent B）
- [x] 批次 4 Theme Token：Theme.ets 14→50 常量 + 7 存量对话框迁移（Agent C）
- [x] 批次 4 对话框：新建 12 个对话框（Agent D + Agent E）
- [x] 集成：12 对话框挂载 MainLayout（L1567-1578）+ 12 @State + dispatch 重接
- [x] Agent G：再注册 11 命令 + Qwt 5 API 兼容修复 + 补 4 头文件
- [x] string.json 两次损坏修复（406→492→511 条三语言对齐，覆盖 0 缺失）
- [x] 代码审查：1 CRITICAL + 7 MAJOR 定位
- [x] 修复落地：Qt 侧新命令 + ArkTS 侧 M-5 接线 + M-6 补 i18n key
- [x] 构建：ninja 通过 + libentry.so（104.8MB）复制 + assembleHap SUCCESS（18.6s）
- [x] 真机侧载验证：install/start/链路 hilog/verify_smoke 6/6 PASS

## 技术决策

- **决策 1（命令注册模式）**: 10 个变更类命令全部 fire-and-forget（不回传结果）；查询类命令加入 queryCommands() 走读取路径。
- **决策 2（复用现成类）**: 新增命令尽量复用 Qt 现成实现（Convolution/normalizeColumns/Graph::addTimeStamp/addImage/ArrowMarker），不重复造轮子。
- **决策 3（对话框架构）**: 对话框一律 ArkTS 实现 + @Link @Watch isVisible 无条件挂载，避免重复创建与生命周期问题。
- **决策 4（string.json 编辑纪律）**: string.json 严禁多 agent 并行编辑（本次两次损坏教训），改为串行编辑或脚本合并（git HEAD 合并 / 提取恢复）。
- **决策 5（Qwt API 差异）**: Qwt 5 与 Qwt 6 API 存在差异（Log10 vs Logarithmic），移植时需按 Qt 工程实际版本核对。

## 文件变更

| 文件 | 变更类型 | 说明 |
|------|---------|------|
| `scidavis/scidavis/src/main.cpp` | 修改 | 批次 2 注册 10 命令（L1918-2147）+ queryCommands 补 2 查询（L619）；Agent G 再注册 11 命令（L2156-2496）+ 5 查询入 queryCommands；Qwt 5 API 兼容修复 + 补 4 个头文件 |
| `Theme.ets` | 修改 | 常量 14→50（对话框表面/强调色/间距/圆角/阴影/字号/按钮/输入框/Tab），迁移 7 个存量对话框 |
| `MainLayout.ets` | 修改 | 菜单树 rebuildMenus（Scripting/Format/Tools + Plot/Analysis 子菜单层级 + File→New）+ 12 对话框挂载（L1567-1578）+ 12 个 @State + dispatch 重接（add_text/draw_line/draw_arrow/add_image/format_*） |
| `BottomToolBar.ets` | 修改 | isTable 动态化（表/图按钮组切换） |
| `TableOpsDialog.ets` | 修改 | 挂入 TableSizeDialog |
| `string.json`（en_US / zh_CN） | 修改 | 两次损坏修复（406→492→511 条三语言对齐），M-6 补齐 93+19 i18n key，getAppString 覆盖 0 缺失 |
| 12 个新对话框（PlotWizardDialog/AxesDialog/AssociationsDialog/CurveRangeDialog/TextDialog/LineDialog/ImageDialog/SymbolDialog/LayerDialog/RenameWindowDialog/FindDialog/TableStatisticsDialog） | 新增 | 批次 4 对话框新建 |
| `entry/libs/arm64-v8a/libentry.so` | 修改 | ninja 产物复制（104.8MB） |

## 遗留问题

- SymbolDialog 输出未真正分发（dead UI 但已可达）
- updatePlotAssociation 为降级实现（对话框只发显示字符串）
- TextDialog/ImageDialog 样式参数 Qt 侧未消费（addText 只读 text、addImage 只读 filePath）
- CurveRangeDialog 的 AppStorage 'curveRangeIdx' 未写入
- TableStatisticsDialog 列选择为装饰性（targets 未传）
- getMatrixData 无 ArkTS 消费者
- PlotWizardDialog 的 dlg_pw_need_y 在 xCol 为空时也显示
- 3D/打印/模板/脚本为远期项
- 对话框本体 Win 截图仍未补拍（4.3~4.5 视觉验收待补拍）

## 环境信息

- 项目: scidavis-ohos
- 设备: HUAWEI MatePad 11.5（192.168.0.108:46817）
- 日期: 2026-08-02 晚 ~ 08-03 凌晨
- 依赖变更: 无

## 后续建议

1. 优先补齐降级/装饰性实现：SymbolDialog 真实分发、updatePlotAssociation 参数透传、TextDialog/ImageDialog 样式参数 Qt 侧消费。
2. 处理未接线项：CurveRangeDialog 写入 AppStorage 'curveRangeIdx'、TableStatisticsDialog 传递 targets、getMatrixData 挂 ArkTS 消费者、修正 dlg_pw_need_y 显示条件。
3. 用户补拍对话框本体 Win 截图，解锁 4.3~4.5 视觉验收。
4. string.json 后续任何编辑必须串行执行或走脚本合并，杜绝多 agent 并行写坏。
5. 远期项（3D/打印/模板/脚本）单独排期，不在批次 4 范围内追加。
