# 2026-08-03 工作日志：菜单树全量对齐 Win 2.7.2

> 任务：按用户 Win 端截图，将平板端 12 个菜单（含子菜单/快捷键/分隔线）全量对齐 SciDAVis 2.7.2，减少平台转换成本；对齐后菜单打开的对话框 UI 一并接线。
> **核心成果**：①rebuildMenus() 全量重构（File/Edit/View/Scripting/Table/Plot/Analysis/Graph/Matrix/Windows/Help 12 菜单）；②统计项从 Table 迁入 Analysis（用户确认）；③604 i18n 键三语言对齐；④4 个新对话框（FormatDialog/ToolsDialog/TableSetAsDialog/TableFillDialog）创建并接线；⑤真机验证菜单结构与对话框可达性全部通过。

---

## 工作内容

1. **备份**：robocopy backup-20260803-1649-ohos-full（排除 hilog/build/oh_modules/.hvigor/.cache/.git/diagnostics，1.13GB/4815 文件）。
2. **差异分析**（读 70 张 Win 截图 + 平板 rebuildMenus 实况）：12 菜单差异矩阵——Format/Tools 已对齐；File/Edit/View/Table/Plot/Analysis/Graph/Matrix/Windows/Help 需重构。
3. **Agent A（菜单树对齐，22min）**：rebuildMenus() 全量重构：
   - File：New▸级联（7 项+快捷键）+ 打开图像文件/导入图像/Recent/Template×2/Export Graph▸/Print All Plots
   - Edit：补 Delete Fit Tables/Clear Log Info，Redo 改 Ctrl+R
   - Table：16 项全量（Set As▸/Fill▸/Formula/Change Type/Clear/Convert 等），**统计项移除**
   - Analysis：**统计项迁入**（Table 态）+ Autocorrelate/Deconvolute；图形态 Translate▸+顺序重排+Fit Wizard 平级底部 Ctrl+Y
   - Plot：补 Vectors XYXY/XYAM，Panel/3D 子菜单化
   - Graph：挂 Add Text/Draw Arrow/Draw Line/Add Image/New Legend/Layout/Layer（Alt/Ctrl 快捷键）
   - Matrix：15 项全量（Mirror/Determinant/Convert 等）；Windows：Tile/Next/Previous/Duplicate/Geometry/Hide；Help：8 项补全
   - 未适配项统一 enabled:false + showNotAdapted
4. **Agent B2（新对话框，46s）**：FormatDialog（plot/scales/axes/grid/title 五合一）、ToolsDialog（move/remove）、TableSetAsDialog、TableFillDialog。
5. **Agent B1（挂载接线，4min）**：4 对话框挂载 + 8 类新 dispatch case（set_columns_as/fill_selection/move_points/remove_bad/format_*等）。
6. **质量修复（编排者）**：
   - string.json en_US/zh_CN JSON 括号损坏修复（`]]`→`]`，A 编辑失误）
   - File 菜单底部错误项修复（去 close/close_all，exit→quit 用 file_quit）
   - FormatDialog 15 个 ArkTS 编译错误修复（Picker 组件不存在→ForEach/Slider、placeholder 入构造器、switch→if 链、显式类型常量）
   - 5 个缺失 i18n 键补齐（tools_move_points_desc/tools_move_hint/tools_remove_bad_desc/format_line_color/format_line_style）→ 604 条三语言一致
7. **构建**：命令行 hvigor BUILD SUCCESSFUL（14s）。
8. **真机验证**（hdc uinput/dumpLayout）：
   - 菜单栏结构：Table 态（9 项）与图形态（File/Edit/View/Scripting/Graph/Tools/Format/Analysis/Windows/Help 10 项）均与 Win 一致
   - File 菜单：New▸级联展开 + 打开图像文件/导入图像/Recent/Template/Print All Plots 齐全
   - Edit：撤消/重做 Ctrl+R/剪切/复制/粘贴/删除 Del/Delete Fit Tables/Clear Log Info
   - Analysis：对列/行统计（迁入成功）+ FFT/关联/Autocorrelate/Convolute/Deconvolute/拟合向导
   - Windows：级联/Tile/Next/Previous/重命名/Duplicate/Geometry/Hide/关闭
   - 图形态 Graph/Tools/Format 菜单：10 项图形态完整
   - **FormatDialog 打开成功**（标题 section + 取消/应用）；**ToolsDialog 打开成功**（描述文字正常显示，键名修复确认）

## 完成项

- [x] 备份（backup-20260803-1649-ohos-full）
- [x] 差异分析（12 菜单矩阵）
- [x] Agent A：菜单树全量对齐 + 604 i18n 键
- [x] Agent B2：4 个新对话框
- [x] Agent B1：挂载 + dispatch 接线
- [x] 质量修复（JSON 括号/File 底部/FormatDialog 编译/i18n 补键）
- [x] 构建 BUILD SUCCESSFUL
- [x] 真机验证（菜单结构 + FormatDialog/ToolsDialog 可达）
- [x] 分两批提交（01cab63 菜单对齐 / eefee84 新对话框）

## 技术决策

- **决策 1（统计项归属）**: Statistics on Columns/Rows 从平板 Table 菜单迁入 Analysis 菜单（Table 态），与 Win 一致（用户确认）。
- **决策 2（未适配项）**: Win 有但平板无引擎能力的项（模板/打印/Tile/Duplicate/Help 外链/3D 等）统一 enabled:false + showNotAdapted 灰显，保证菜单结构 100% 对齐而非隐藏。
- **决策 3（New 级联）**: new_function_plot/new_3d_surface 从平级移入 File▸New 子菜单（Win 结构），3D 保留 disabled。
- **决策 4（对话框复用）**: Format 菜单 5 项用 FormatDialog 单组件 section 切换，避免 5 个重复文件。

## 文件变更

| 文件 | 变更类型 | 说明 |
|------|---------|------|
| `entry/src/main/ets/pages/MainLayout.ets` | 修改 | rebuildMenus 12 菜单全量重构 + 4 对话框挂载 + 8 类 dispatch case（+385 行） |
| `string.json`（base/en_US/zh_CN） | 修改 | 599→604 键（菜单项 + 对话框描述），三语言对齐 |
| `dialogs/FormatDialog.ets` | 新增 | 格式对话框五合一（plot/scales/axes/grid/title） |
| `dialogs/ToolsDialog.ets` | 新增 | 工具对话框（move/remove） |
| `dialogs/TableSetAsDialog.ets` | 新增 | 列角色对话框（X/Y/Z/xErr/yErr） |
| `dialogs/TableFillDialog.ets` | 新增 | 填充对话框（行号/随机/正态） |

## 遗留问题

1. **子菜单展开交互**：New▸ 等子菜单在触摸设备上需点击项本体展开（hover 不生效），已实测可用但 UX 待优化（可加"点击箭头展开"）。
2. **菜单语言**：应用重启后菜单语言可能回英文（base 语言），与系统语言联动待确认（zh_CN 资源已就绪）。
3. **未适配项清单**：模板/打印/Tile/Duplicate/Window Geometry/Hide/3D/Help 外链等灰显占位，待引擎能力补齐后启用。
4. **真机视觉验收**：菜单结构已 dump 验证，但"与截图并排视觉对比"建议用户肉眼确认（菜单项文字与 Win 截图一致性）。

## 环境信息

- 项目: scidavis-ohos（ohos 仓库）
- 设备: HUAWEI MatePad 11.5（192.168.0.108:46817）
- 日期: 2026-08-03
- 提交: 01cab63（菜单对齐）/ eefee84（新对话框）
- 依赖变更: 无

## 后续建议

1. 用户肉眼对比平板 vs Win 截图，确认菜单项文字/顺序 100% 一致，报告差异项。
2. 若发现差异，按差异清单小步修复（纯 rebuildMenus 调整，低风险）。
3. 补拍 Win 对话框本体截图（20 个），解锁对话框视觉对齐（FormatDialog 等新对话框的逐字段对照）。
