# 2026-08-01 工作日志：Qt 原生表格恢复（撤销 ArkTS 覆盖层自动切换）

> 用户人工复核发现上一轮"自动切 tables 视图"方向错误：打开软件显示的是 ArkTS 糊上去的
> 简陋表格（TablePage+ColumnSidebar）而非 Qt 原生渲染。用户确认"新建矩阵+拖动矩阵标题"
> 可让 Qt 表格恢复（证明 setViewportMargins 遮挡修复有效），要求：打开即 Qt 原生表格，
> 侧边栏问题之后再解决。按纪律先全量备份 ohos 再修复。

---

## 工作内容

1. **全量备份**：`backup-20260801-0315-ohos-full/`（43.5GB，robocopy /E /MT:16，命名对齐现有 backup- 规范）。
2. **log 分析 + 机制确认**（deep agent）：
   - 打开即 ArkTS 覆盖表格：启动链路（main.cpp:1901 newTable → refreshUiState 读到 activeType='Table'）触发 MainLayout.ets:385-387 自动切 'tables' → TablePage 覆盖 Qt。
   - "新建矩阵恢复 Qt"机制：Matrix 成为活动窗口 → 某次 refreshUiState（工具栏 700ms 延迟刷新是主路径）命中 :392 回退 'default' → TablePage 卸载 → Qt 原生露出。"有概率" = 700ms 时序竞态（Qt 慢则滞留）+ 菜单入口无事后刷新。
   - setViewportMargins 遮挡修复独立生效（纯 Qt 几何，与视图无关），用户确认有效。
3. **最小修复**：删除 MainLayout.ets:385-387 自动切 tables 三行，currentView 恒 'default' 透传 Qt。保留 :392 回退块与 userViewLocked / 显式 View 菜单切换 / restore_window / mask:false 等。
4. **构建部署真机验证**：启动后无 TablePage/ColumnSidebar 日志，布局 dump 27 节点无 sidebar_*，仅 `Table1 (Table)` 窗口 → **Qt 原生表格直接可见**。

---

## 完成项

- [x] 全量备份（43.5GB）
- [x] 机制确认（打开即 ArkTS 覆盖的完整链路 + 恢复机制）
- [x] 删除自动切 tables（MainLayout.ets:385-387，arkts_check ✅）
- [x] 构建部署 + 真机验证（Qt 原生表格可见，无覆盖）
- [x] 截图留档 diagnostics/screenshots/20260801-qt-native-table.png

## 技术决策

- **决策 1**: 撤销自动切 tables 视图。ArkTS TablePage 覆盖层仅保留为 View 菜单的显式功能；默认视图恒透传 Qt 原生渲染。
- **决策 2**: setViewportMargins 遮挡修复**保留**——它独立于视图层，是 Qt 表格可见性正确的必要条件，用户已确认有效。
- **决策 3**: 侧边栏（ColumnSidebar）问题推迟——当前优先级是 Qt 表格正常；后续可在 'default' 视图上叠加 ColumnSidebar 而非恢复自动切 tables。

## 文件变更

| 文件 | 变更类型 | 说明 |
|------|---------|------|
| `ohos/entry/src/main/ets/pages/MainLayout.ets` | 修改 | 删除 refreshUiState 中自动切 tables 三行（保留 :392 回退 + 显式切换） |
| `backup-20260801-0315-ohos-full/` | 新增 | 全量备份快照 |

## 遗留问题

- 侧边栏（ColumnSidebar）在默认视图不显示——按用户要求推迟，Qt 表格正常后再解决。
- 问题4（工具栏右键）/问题5（窗口控制）按用户指示搁置维持现状。
- 建议：侧边栏后续方案 = 在 'default' 视图叠加右侧 ColumnSidebar（不替换 Qt 表格），需新增 Qt 选中事件→ArkTS 桥。

## 环境信息

- 项目: scidavis-ohos
- 设备: HUAWEI MatePad 11.5（OpenHarmony-7.0.0.32, API 26, 192.168.0.116:46817）
- 日期: 2026-08-01
- Qt: 5.15.12；libentry.so 102,605,680 字节（03:00 重建，含 setViewportMargins）
- 依赖变更: 无

## 后续建议

1. 真机人工确认：打开即 Qt 原生表格、可选中/可输入/可拖拽；新建矩阵/注释/图形正常。
2. 确认遮挡解决（首行可见）——setViewportMargins 已生效。
3. 侧边栏方案：作为 'default' 视图右侧覆盖（不替换 Qt 表格），需 Qt 选中事件桥；确认需求后再实施。
