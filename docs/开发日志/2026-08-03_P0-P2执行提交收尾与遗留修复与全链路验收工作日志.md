# 2026-08-03 工作日志：P0-P2 执行（提交收尾 + 批次4遗留修复 + 全链路验收）

> 任务：按上一会话工作指导执行 P0（提交收尾）→ P1（7 项遗留接线修复）→ P2（全链路真机验收）。
> **核心成果**：①08-03 成果分批提交 3 commit；②R1 并行修复 7 项缺陷全部落地（Qt 3 命令参数消费 + 4 对话框接线 + MatrixPage 新建 + MainLayout 集成）；③构建通过 + verify_smoke 6/6 PASS + 真机启动链路确认；④产出 18 项验收矩阵（14✅/3⚠️/1❌）。

---

## 工作内容

1. **P0 提交收尾**：查看 37 文件工作区清单，分 3 批提交（39fc740 libentry.so 命令产物 / 32ab467 批次4 UI / 2a93da2 文档 + AGENTS.md 死链修复），修复 AGENTS.md 死链（DEVELOPMENT_GUIDE.md → docs/README.md）。
2. **探索定位**：3 个 explore agent 并行收集 7 项缺陷精确根因（ArkTS 对话框 + Qt main.cpp + 矩阵消费者三路）。
3. **R1 并行修复（7 agent）**：
   - Qt（deep）：updatePlotAssociation 真实实现（DataCurve::setXColumnName/loadData）+ addText 样式消费（Legend setFont/setTextColor/setFrameStyle）+ addImage 几何消费（ImageMarker setRect/keepAspect）；staging 同步 SHA256 一致
   - TableStatisticsDialog：targets 传递 + @Prop statType + fire-and-forget 响应处理
   - CurveRangeDialog：AppStorage/curveIdx/0 三段式兜底
   - PlotWizardDialog：xCol/yCol 独立校验（need-x 提示）
   - SymbolDialog：onOk 分发 addText
   - AssociationsDialog：colRoles 结构化输出（tableName/xCol/yCol/xErr/yErr）
   - MatrixPage 新建：getMatrixData 只读网格消费者
4. **R2 集成（1 agent）**：MainLayout 统一接线（statType 传参 / curveRangeIdx 写入 / currentView 'matrix' 分支 + Matrix 自动切换）
5. **质量修复**：TableStatisticsResponse 接口补 queued 字段（arkts_check 通过）
6. **构建**：ninja 编译通过（POST_BUILD cp 报错属预期）→ libentry.so 104.8MB 复制 → hvigor assembleHap BUILD SUCCESSFUL（11.7s）
7. **真机验证**：hdc install -r + OHPlotAbility 启动 → hilog 完整链路（Qt 主窗口 → getUiState 返回 Table1）→ verify_smoke.py 6/6 PASS
8. **P2 验收**：hdc uinput/uitest 触摸注入 + dumpLayout 坐标确认 + hilog 留证，产出验收矩阵 18 项

## 完成项

- [x] P0：3 批提交 + AGENTS.md 死链修复
- [x] P1：7 项遗留缺陷修复（Qt 3 命令 + 4 对话框 + MatrixPage + MainLayout 接线）
- [x] 构建：ninja + hvigor 全通过
- [x] 验证：verify_smoke 6/6 + 真机启动链路
- [x] P2：验收矩阵 18 项（14✅/3⚠️/1❌）

## 技术决策

- **决策 1（Qt 参数消费）**：updatePlotAssociation 参考 AssociationsDialog::changePlotAssociation，用 DataCurve::setXColumnName/setYColumnName/loadData 真实重关联；错误棒列降级为 qWarning（无 UI 弹窗，C1 约束）。
- **决策 2（addText/addImage）**：用 newLegend/addImage 返回指针后调 setter 应用样式/几何，alignment 无 Qt 等价物显式忽略。
- **决策 3（矩阵）**：MatrixPage 只读渲染（setMatrixCell 未注册），表头=列序号，字段名 cells（非 data/colNames）。
- **决策 4（对话框按钮可达性）**：验收发现 SymbolDialog 确定按钮点击未触发 onOk（详见遗留问题），疑似按钮区域被 Qt 层事件拦截。

## 文件变更

| 文件 | 变更类型 | 说明 |
|------|---------|------|
| `scidavis/scidavis/src/main.cpp` + `tools/qt_src_staging/main.cpp` | 修改 | updatePlotAssociation/addText/addImage 参数消费修复；staging 同步 SHA256 一致（A116080E） |
| `TableStatisticsDialog.ets` | 修改 | targets 传递 + statType prop + queued 字段 |
| `CurveRangeDialog.ets` | 修改 | curveIdx 三段兜底 |
| `PlotWizardDialog.ets` | 修改 | xCol/yCol 独立提示 |
| `SymbolDialog.ets` | 修改 | onOk addText 分发 |
| `AssociationsDialog.ets` | 修改 | colRoles 结构化输出 |
| `MatrixPage.ets` | 新增 | getMatrixData 消费者（230 行） |
| `MainLayout.ets` | 修改 | statType/curveRangeIdx/matrix 视图接线 |
| `entry/libs/arm64-v8a/libentry.so` | 修改 | ninja 产物（104.8MB） |
| `diagnostics/acceptance/2026-08-03_全链路验收矩阵.md` | 新增 | 18 项验收矩阵（gitignore） |

## 遗留问题

1. ~~**❌ SymbolDialog 确定按钮未分发 addText**~~ → **已解决（16:29 真机确认 ✅）**：原判"断裂"为假象——真机当时运行 01:47 旧 HAP（不含 R1 修复）。根因：16:05 build_project 构建输出误入 `C:\Users\Forwardz\Desktop\app-discord\`（错误项目目录，204KB 空壳）。改用命令行 hvigor 在 ohos 目录构建后暴露并修复 3 个 ArkTS 编译错误，重新侧载后 onOk→addText 完整链路工作（hilog cid=18: `scidavis_call(cmd=addText) args={"text":"∀"} -> success`）。
2. **⚠️ 查找窗口文本输入**：uinput -K 无法注入 Qt 侧 TextInput，find 命令未确认触发（待蓝牙键盘验证）。
3. **R1 修复真机待验项（3 项）**：updatePlotAssociation/setCurveRange 需图形含曲线；tableStatistics targets 需选列操作。建议 Win 端建带曲线示例项目后复验。
4. **INSTRUCTION_FOR_AI.md 未提交**：用户放置的 AI 指令文件，非仓库资产，保持排除。
5. **qwen3.5-35b-a3b 配额耗尽**：本次 4 个 agent 回退 sensenova-6.7-flash-lite 完成（功能正常，注意配额）。
6. **build_project 工具目录污染**：16:05 的 build_project 调用将产物输出到 `C:\Users\Forwardz\Desktop\app-discord\`（错误项目上下文），导致假成功。后续构建一律用命令行 hvigorw 在 ohos 目录执行。

## 环境信息

- 项目: scidavis-ohos（ohos 仓库，main 分支）
- 设备: HUAWEI MatePad 11.5（192.168.0.108:46817）
- 日期: 2026-08-03
- 提交: 39fc740 / 32ab467 / 2a93da2 / ac649d6 / e94d31b
- 依赖变更: 无

## 后续建议

1. ~~优先排查 SymbolDialog 确定按钮事件被 Qt 层拦截~~ → 已解决（旧 HAP 假象）。
2. 建带曲线/矩阵的示例项目，复验 updatePlotAssociation/setCurveRange/MatrixPage 真机链路。
3. 补拍 Win 对话框本体截图，解锁 4.3~4.5 视觉验收。
4. string.json 后续编辑必须串行（本次未触碰，纪律保持）。
