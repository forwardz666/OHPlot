# 2026-08-01 下一步工作分析：短平快实现桌面端 80% 功能

> **报告性质**：规划分析（只读盘点 + 差距矩阵 + 短平快排序），不涉及代码修改。
> **数据来源**：`docs/功能计划/功能实施计划.md`（F-01~F-32 全景）、`docs/下一阶段/*`（qtforohos 分析/AirStars 方案/执行规格）、
> `docs/开发日志/2026-07-27~08-01`（进度历史）、explore agent 对 `main.cpp` 命令注册表与 ArkTS 组件实况的全量扫描（2026-08-01）。
> **决策前提**：维持 alpha_v6 + startQtNative 现有架构（qtforohos 报告 §6 + AirStars 方案 §7 已定论，勿再动摇）；
> **3D 功能远期适配**（F-30，用户明确排除在 80% 之外）。

---

## 一、现状盘点（2026-08-01 实测）

### 1.1 命令通道：55 个已注册命令（`scidavis/scidavis/src/main.cpp` L628-1771）

**查询（12，同步）**：ping / getTableList / getTableData / getColumnInfo / getPlotList / getPlotData / getUiState / getClipboardText / getGraphCurves / getProjectTree / getPreference / getNoteData

**变更（43，fire-and-forget）**：newTable / setCellValue / addTableColumns / setColumnValues(列公式 muParser) / sortTable / tableStatistics(行列统计) / plot2D(13 种图型) / importASCII / exportASCII / exportGraphImage(PNG) / matrixSetDimensions / matrixSetFormula / matrixTranspose / matrixInvert / analyzeCurve(11 种 op：fit_linear/sigmoidal/gauss/lorentz/poly、differentiate/integrate/fft/smooth/interpolate/fft_filter) / correlate / openProject / saveProject / setGraphTitle / setAxisTitle / setAxisScale / toggleLegend / toggleColumnEditor / activateWindow / rescale / graph_pointer / zoom_in / zoom_out / screen_reader / data_reader / new_function / add_curve / add_error_bars / add_function / setClipboardText / getClipboardText / setPreference / setChromeInsets / getNoteData / setNoteData / insert_row / insert_col / menuAction(19 个 itemId)

### 1.2 ArkTS UI（`ohos/entry/src/main/ets/`）

- **页面**：MainLayout(主框架) / TablePage / PlotPage / NoteEditor / SubWindowBlank / Index
- **组件**：MenuBar / ToolBar(顶部 File+Edit+Graph 三组) / BottomToolBar(底部 Plot+Plot3D 两组) / StatusBar / DataTable / PlotCanvas / ProjectTree / ContextMenu / ColumnSidebar(**存在但未挂载**) / QtEventHost / Theme / LocaleHelper
- **对话框(10)**：Preferences / Import / Export / Plot2D / GraphProps / Analysis(含拟合参数) / MatrixOps / ScriptConsole / TableOps / About
- **菜单结构**：File(14 项) / Edit(9 项) / View(7 项) / 上下文菜单(随活动窗口类型 Table/Matrix/Graph 切换) / Plot(17 项) / Analysis(14 项) / Windows(cascade+动态窗口列表) / Help(2 项)
- **文件 IO 闭环已通**：DocumentViewPicker + 沙箱拷贝 + openProject/saveProject/importASCII/exportASCII
- **绘图渲染**：PlotPage + PlotCanvas（getPlotData 数据 → ArkTS Canvas 重绘，路线 B）
- **交互**：触摸/鼠标转发、长按=右键、右键菜单、窗口控制键(—/□/✕)、项目树、结果日志面板

### 1.3 结论

**桌面端核心功能（表格/绘图/分析/矩阵/文件/笔记/UI 框架）已全部打通命令层与 UI 框架**，
AirStars 方案 §7.3（07-31）"六大模块已通、剩余 10-16 人天"的评估仍然成立。
距"80% 功能"的差距集中在**三类**：①已接入 UI 但灰显未接线；②命令缺失但 Qt 引擎类现成；
③已实现功能的真机验收（防"命令注册但 UI 断裂"）。

---

## 二、差距矩阵（桌面端功能 vs 当前实现）

| 桌面端功能 | 当前状态 | 差距类型 | 工作量 |
|-----------|---------|---------|-------|
| **图编辑**：add_curve / add_error_bars / add_function / rescale | 命令**已注册**，Graph 菜单灰显 | ① UI 启用+接线 | ★ 纯 ArkTS |
| **图 enrichment**：add_text/draw_arrow/draw_line/time_stamp/add_image/new_legend | 工具栏已接入，命令**未注册** | ② 补命令+对话框 | ★★ |
| **归一化 normalize** | 命令**未注册**（Qt 类现成） | ② | ★ |
| **卷积 convolve** | 命令**未注册**（Qt Convolution 类现成） | ② | ★ |
| **矩阵数据读取 getMatrixData** | **未注册**（有 set/维度/公式/转置/求逆，无读） | ② | ★（复用 DataTable） |
| **列属性侧边栏 ColumnSidebar** | 组件**存在未挂载**（toggleColumnEditor 已注册） | ③ 挂载+接线 | ★★ |
| **分析对话框完善**（AnalysisDialog 参数面板对齐桌面） | 对话框存在，参数完整度待真机核对 | ③ 验收 | ★★ |
| **已注册命令链路验收**（55 命令→UI 逐个点通） | 大部分未在 08-01 后系统验收 | ③ 真机验证 | ★★ |
| **打印 Print** | 灰显；OHOS 无打印框架 | 降级：导出 PDF | ★★（可选） |
| **模板 open_template/save_template** | 灰显 | 远期 P3 | - |
| **3D 绘图 plot3d_*** | 灰显（用户明确**远期**） | 远期 F-30 | - |
| **脚本 Script Console** | 灰显桩 | 远期 | - |
| **Qt 离屏渲染 renderPlot** | 未实现；PlotCanvas 路线 B 已工作 | 可不做（路线 B 满足） | - |

---

## 三、80% 目标定义

以桌面端 SciDAVis 菜单功能项为分母（约 100 项可交互功能，含命令层+UI），
**实现"①+②+③"三批后可达约 90% 功能覆盖**（3D/打印/模板/脚本约 10-15% 留远期）：
- **8 大菜单结构完成度**：当前已 ~85%（仅 print/help/3D/模板灰显）
- **命令层完成度**：当前 ~80%（缺 normalize/convolve/getMatrixData/enrichments ≈ 7 个命令）
- **UI 接线完成度**：Graph 菜单灰显 4 项 + enrichments 6 项待接线

---

## 四、下一步工作清单（短平快排序，按 ROI）

### 批次 1：图编辑灰显落地（纯接线，半天级）
| # | 任务 | 说明 |
|---|------|------|
| 1.1 | Graph 菜单启用 add_curve/add_error_bars/add_function/rescale | 命令已注册，仅解除 MENU 灰显 + dispatch 验证 |
| 1.2 | 真机验证 4 项命令的对话框链路（CurvesDialog 由 QtEventHost 接管） | 防"命令注册但 UI 断裂" |

### 批次 2：缺失命令补齐（main.cpp + ArkTS，1-2 天）
| # | 任务 | 说明 |
|---|------|------|
| 2.1 | 新增 `normalize` 命令（归一化：列/表，复用 Qt 现成类） | 命令 + 对话框（可并入 TableOpsDialog） |
| 2.2 | 新增 `convolve` 命令（自/互相关扩展，复用 Convolution 类） | 命令 + 对话框（并入 AnalysisDialog 或独立） |
| 2.3 | 新增 `getMatrixData` 命令（矩阵数据读取） | 命令 + 复用 DataTable 渲染矩阵 |
| 2.4 | 新增 enrichment 6 命令（add_text/draw_arrow/draw_line/time_stamp/add_image/new_legend） | 工具栏已接入，补命令分发 + 最少参数 |

### 批次 3：功能验证与收尾（1-2 天）
| # | 任务 | 说明 |
|---|------|------|
| 3.1 | 挂载 ColumnSidebar（列属性侧边栏，组件已存在） | toggleColumnEditor 命令已注册，接通显示 |
| 3.2 | 55 命令全链路真机验收（逐菜单点通，hilog 留证） | 产出验收矩阵，标记断裂项 |
| 3.3 | 分析对话框参数面板对齐桌面版（AnalysisDialog 逐字段核对） | 拟合/FFT/滤波/插值/平滑参数完整度 |
| 3.4 | 导出 PDF 降级方案（print 灰显 → File▸Export PDF 可用） | 可选，exportGraphImage 扩展 |

### 远期（明确排除在 80% 外）
- 3D（F-30，用户指定远期）；打印（如 3.4 不做则留远期）；模板（F-32）；脚本（F-32）

---

## 五、建议的验收基线

每个功能沿用既有 DoD（功能实施计划 §九）：
1. `hvigorw assembleHap` 零 ERROR；
2. 真机四维验证（启动 / 输入 / 15min 稳定 / 交互响应）；
3. 与 Windows 版同操作同屏对比截图存档（`diagnostics/`）；
4. 新增命令同步更新功能实施计划 §1.2 命令清单；
5. 每批次独立提交（用户确认后）。

**关键路径提醒**：批次 3.2（全链路验收）应尽早启动——它能暴露"命令已注册但 UI 未接线"的隐藏断裂，
避免在断裂地基上继续堆功能。

---

## 六、环境与决策锚点（勿动）

- 架构：alpha_v6 QPA + startQtNative + ArkTS 覆盖层 + JS 桥（qtforohos 报告 §6 / AGENTS.md 规则 5）
- 构建：ninja（build-ohos）→ 复制 libentry.so → hvigor → `hdc install -r`
- 设备：HUAWEI MatePad 11.5（OpenHarmony-7.0.0.32, API 26, 192.168.0.116:46817）
- 硬约束：C1 对话框必须 ArkTS；C2 变更命令 fire-and-forget；C7 沙箱文件流（AGENTS.md + 功能计划 §1.3）
