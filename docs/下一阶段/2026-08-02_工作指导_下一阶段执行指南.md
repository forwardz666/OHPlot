# 2026-08-02 工作指导：OHPlot 下一阶段执行指南（交接文档）

> **用途**：下一次对话的工作指导。整合全部规划（80% 短平快计划 + UI 对齐规格书 + 截图盘点 +
> 委派提示词），按优先级排定执行顺序，新会话直接按此推进，无需重新调研。
> **上游文档**：`docs/下一阶段/2026-08-01_80%功能短平快实施计划.md`（规划主文档）、
> `docs/下一阶段/2026-08-01_UI对齐Windows规格书.md`（UI 对齐基准）、
> `docs/下一阶段/UI对齐委派提示词.md`（批次 4 委派提示词库）、
> `docs/下一阶段/2026-08-01_五类用户反馈问题根因分析.md`（P0 修复记录）。

---

## 一、当前状态快照（2026-08-02 收尾）

### 已完成
- ✅ **批次 1**：图编辑三功能（add_curve/add_error_bars/add_function 参数化命令 + ArkTS 对话框），真机验证通过
- ✅ **批次 4 菜单类截图**：补拍 17 张完成（Graph/Tools/Format/3D Plot/Matrix/Analysis 图形态/File→New），
  菜单类共 70 张截图全部视觉识别完毕
- ✅ **工具链**：multimodal-looker 强制绑定 `aliyun/MiniMax-M2.5`（4 处配置源 + fallback_models 清空）；
  subagent 监管配置（`~/.omo/omo.jsonc` 的 `[opencode]` 块）
- ✅ **文档**：80% 计划含批次 4 + 截图补拍清单（§7）；UI 对齐规格书修正（动态菜单模型）；委派提示词 8 份

### 关键认知（勿再动摇）
- **动态菜单**：2.7.2 = 6 常驻（File/Edit/View/Scripting/Windows/Help）+ 窗口类型追加
  （Table→Plot/Analysis/Table；MultiLayer→Graph/Tools/Analysis/Format；Matrix→3D Plot/Matrix）
- **无 "Plot Data" 菜单**：`plotDataMenu` 变量标题即 **"Tools"**
- **OH 菜单已正确**：`MainLayout.ets rebuildMenus()` 已是动态菜单，无需改结构
- **架构定论**：alpha_v6 QPA + startQtNative + ArkTS 覆盖层 + JS 桥

---

## 二、下次执行顺序（按优先级）

### 第 1 步：截图补拍收尾（用户操作，解锁对话框对齐）
- 待补拍：**对话框本体 20 个 + Graph 菜单中段 1 张 + 主窗口全貌 1 张**（清单见 80% 计划 §7.2）
- 关键前置：**必须先在 Win 端激活图形窗口**（File▸New▸Graph）才能打开 Graph/Format 系对话框
- 补拍后放入 `diagnostics/ui-alignment/2_对话框/` 对应子目录

### 第 2 步：批次 2 命令补齐（不依赖截图，可立即并行）
| # | 任务 | 说明 |
|---|------|------|
| 2.1 | `normalize` 命令 | 复用 Qt 现成类；对话框并入 TableOpsDialog |
| 2.2 | `convolve` 命令 | 复用 Qt Convolution 类；并入 AnalysisDialog |
| 2.3 | `getMatrixData` 命令 | 矩阵数据读取；复用 DataTable 渲染 |
| 2.4 | enrichment 6 命令 | add_text/draw_arrow/draw_line/time_stamp/add_image/new_legend；工具栏已接入 |
| 2.5 | **table_size 命令（优先）** | 底部工具栏按钮已就位（弹"未适配"），Qt 侧补行列数设置命令 + ArkTS 对话框 |

### 第 3 步：批次 3 功能验证
| # | 任务 | 说明 |
|---|------|------|
| 3.1 | 挂载 ColumnSidebar | 组件已存在未挂载；toggleColumnEditor 已注册 |
| 3.2 | **55 命令全链路真机验收（关键路径）** | 逐菜单点通，hilog 留证，产出验收矩阵 |
| 3.3 | AnalysisDialog 参数面板对齐 | 拟合/FFT/滤波/插值/平滑参数完整度 |
| 3.4 | 导出 PDF 降级 | 可选 |

### 第 4 步：批次 4 UI 对齐（按委派提示词推进）
| # | 任务 | 委派提示词 | 依赖 |
|---|------|-----------|------|
| 4.1 | 设计 Token 统一 | P4.1 | 无 |
| 4.2 | 菜单树对齐 | P4.2 | 无 |
| 4.3 | 对话框批次 1（PlotWizard/Axes/Associations/CurveRange） | P4.3 | 截图（部分可源码对齐） |
| 4.4 | 对话框批次 2（Plot/Text/Line/Image/Symbol） | P4.4 | 截图 |
| 4.5 | 对话框批次 3（Config 补页/Stats/Rename/Find/Layer） | P4.5 | 截图 |
| 4.6 | 视图面板（PlotWizard/Explorer/Results Log/Undo/Console） | P4.6 | 无 |
| 4.7 | 文件对话框对齐 | P4.7 | 无 |
| 4.8 | 无截图对话框（源码对齐） | P4.8 | 补拍或源码 |

### 远期（明确排除）
3D（F-30）、打印、模板、脚本控制台

---

## 三、遗留事项清单

1. **multimodal-looker 模型**：配置已强制 `aliyun/MiniMax-M2.5`，需**重启 DevEco Code** 生效；重启后验证不再用 kimi-k3
2. **熔断复测**：`~/.omo/omo.jsonc` 配置修正后需重启，重跑轮询熔断测试（预期第 8 次相同调用被 cancel）
3. **文档死链**：AGENTS.md 引用 `docs/DEVELOPMENT_GUIDE.md` 不存在（README 已改指 docs/README.md，AGENTS.md 未改）
4. **libentry.so 97.89MB**：触发 GitHub GH001 警告，LFS/Release 迁移留后续
5. **三对话框过时注释**：AddCurve/ErrorBars/AddFunction 的 aboutToAppear 注释仍写 "conditionally"（实际无条件挂载）
6. **tooltip 右边界 clamp**：右侧按钮 hover 提示可能出屏，未处理
7. **补拍后 4.3~4.5 验收**：依赖用户补拍对话框截图

---

## 四、环境与决策锚点（勿动）

- 项目：scidavis-ohos（ohos 独立 git 仓库，remote: github.com/forwardz666/OHPlot.git main）
- Qt 源码：`C:\Users\Forwardz\scidavis-ohos\scidavis\libscidavis\src\`（在 ohos 仓库外！）
- Qt staging：`ohos/tools/qt_src_staging/main.cpp`（改 main.cpp 后必须 SHA256 同步）
- 构建：ninja（build-ohos）→ 复制 libentry.so 到 `entry/libs/arm64-v8a/` → hvigor → `hdc install -r`
- 设备：HUAWEI MatePad 11.5（OpenHarmony-7.0.0.32, API 26，IP 可能 DHCP 变动）
- 硬约束：C1 对话框必须 ArkTS；C2 变更命令 fire-and-forget；C7 沙箱文件流；NAPI 禁 any/require
- 委派纪律：git 机械操作不委派；对话框无条件挂载；每批次独立提交（用户确认后）
- 截图根目录：`ohos/diagnostics/ui-alignment/`（gitignore，不入库）

---

## 五、快速命令参考

```powershell
# 改 Qt 源码后同步 staging
# 1) 修改 scidavis/libscidavis/src/main.cpp（或 ohos/scidavis 映射处）
# 2) 同步 staging + SHA256 校验
Get-FileHash main.cpp; Get-FileHash tools/qt_src_staging/main.cpp
# 3) ninja 构建 native → 复制 libentry.so
# 4) hvigor 构建 HAP → hdc install -r
```

---

## 六、本会话遗留待确认

- [ ] 用户是否已重启 DevEco Code（multimodal-looker 模型 + 熔断配置生效）
- [ ] 用户是否补拍对话框本体截图（20 个 + Graph 中段 + 主窗口全貌）
- [ ] 是否启动批次 2（建议 table_size 优先，可与补拍并行）
