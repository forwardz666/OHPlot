# 2026-07-30 修复任务清单

> 依据 `docs/下一阶段/AI_ORCHESTRATOR_PROMPT.md` 执行

---

## T0: 修复 Qt 内容被 ArkTS 工具栏遮挡 — P0

| 字段 | 值 |
|------|-----|
| **现象** | Qt 渲染的表格/矩阵/绘图内容被 ArkTS 菜单栏、工具栏、状态栏遮挡 |
| **根因** | `setChromeInsets` 命令在 C++ `commandRegistry()` 中缺失，QMainWindow 的 `setContentsMargins` 从未被调用 |
| **涉及文件** | `scidavis/scidavis/src/main.cpp`（C++ 端注册命令）<br>`ohos/entry/src/main/ets/pages/MainLayout.ets`（ArkTS 端验证） |
| **验收标准** | 1. Qt 内容的上边距对齐 ArkTS 菜单栏+工具栏底部<br>2. Qt 内容的下边距对齐状态栏顶部<br>3. 工具栏高度变化时（行数变化/上下文组切换）内容边距同步更新 |

---

## T1: Windows 菜单窗口列表去重 — P0

| 字段 | 值 |
|------|-----|
| **现象** | Smooth/Derivative/Integrate 后 Windows 菜单中分析结果窗口出现两次 |
| **根因** | `uiStateJson()` 虽有 `QSet<MyWidget*> seen` 去重，但 `windowsList()` 的拼接方式可能导致同一对象以不同指针出现 |
| **涉及文件** | `scidavis/scidavis/src/main.cpp`（C++ 端 `uiStateJson` 函数）<br>`ohos/entry/src/main/ets/pages/MainLayout.ets`（ArkTS 端 `rebuildMenus` 函数） |
| **验收标准** | 1. 执行任一分析操作后，Windows 菜单中每窗口仅出现一次 |

---

## T2: 首选项后端接线验证 — P0

| 字段 | 值 |
|------|-----|
| **描述** | 确认 `getPreference`/`setPreference` 命令已在 C++ 端注册，并检查 `PreferencesDialog.ets` 是否正确调用 |
| **涉及文件** | `scidavis/scidavis/src/main.cpp`（C++ 端命令已注册于 1505-1524行）<br>`ohos/entry/src/main/ets/pages/dialogs/PreferencesDialog.ets`（ArkTS 端） |
| **验收标准** | 1. 打开首选项 → 切换任一设置 → 应用 → 设置写入 QSettings<br>2. 重启应用 → 设置恢复 |

---

## T3: PlotCanvas 多层图支持 — P1

| 字段 | 值 |
|------|-----|
| **描述** | 当前 `PlotPage.ets` 第114行只渲染 `plot.graphs[0]`，但 C++ 端 `getPlotList` 已返回完整的 `graphs[]` 数组 |
| **涉及文件** | `ohos/entry/src/main/ets/pages/PlotPage.ets`<br>`ohos/entry/src/main/ets/components/PlotCanvas.ets` |
| **验收标准** | 1. 多图层 Graph 的每个图层都能正确渲染<br>2. 图层之间坐标轴独立 |

---

## T4: DataTable 列宽自适应 — P1

| 字段 | 值 |
|------|-----|
| **描述** | 当前所有列使用 `flexGrow(1)` 均分宽度，列标题和内容可能被截断 |
| **涉及文件** | `ohos/entry/src/main/ets/components/DataTable.ets` |
| **验收标准** | 1. 列宽根据标题和内容长度自适应<br>2. 列标题完整显示 |

---

## T5: NoteEditor 连接 Qt 后端 — P1

| 字段 | 值 |
|------|-----|
| **描述** | 当前 NoteEditor 是纯本地组件，不保存到 Qt 后端。C++ 端 Note 类已有 `text()`/`setText()` 方法 |
| **涉及文件** | `scidavis/scidavis/src/main.cpp`（注册 `getNoteData`/`setNoteData` 命令）<br>`ohos/entry/src/main/ets/pages/NoteEditor.ets`（ArkTS 端调用命令） |
| **验收标准** | 1. 打开 Note 窗口 → 编辑内容 → 保存项目 → 重新打开 → 内容恢复 |

---

## T6: 分析结果日志自动滚到底部 — P2

| 字段 | 值 |
|------|-----|
| **描述** | 新内容追加后 Scroll 不会自动滚动到最新行 |
| **涉及文件** | `ohos/entry/src/main/ets/pages/MainLayout.ets`（约1050-1080行） |
| **验收标准** | 1. 执行分析操作后自动滚动到最新内容<br>2. 手动滚动时不强制跳转 |

---

## 完成状态

| 任务 | 状态 | 审查 | 备注 |
|------|------|------|------|
| T0: Occlusion fix | ⏳ pending | - | - |
| T1: Windows 菜单去重 | ⏳ pending | - | - |
| T2: 首选项验证 | ⏳ pending | - | - |
| T3: 多层图支持 | ⏳ pending | - | - |
| T4: 列宽自适应 | ⏳ pending | - | - |
| T5: NoteEditor | ⏳ pending | - | - |
| T6: 自动滚动 | ⏳ pending | - | - |